# Recovery File — AdPulse Portal Session 2

**Date:** 2026-03-09
**Session:** Deployment, bug fixes, and Phase 2 planning (auth decoupling, DNS, contributing)

---

## What Was Done This Session

### 1. Branch Merges (Phase 1 agents completed)
All 5 agents from Phase 1 finished. Merged into `development`:
- `feat/ts-cicd` → TypeScript conversion + CI/CD (9 commits, squash merged)
- `feat/api-integration` → API + Auth + tenant support (10 commits, squash merged)
- `feat/ui-polish` → UI polish (3 commits, squash merged)
- Resolved merge conflicts in `package.json`, `package-lock.json`, and page files (.jsx → .tsx renames)

### 2. Cloud Run Deployment
- Deployed QA: `https://adpulse-web-qa-zqlsaqunba-uc.a.run.app`
- Deployed Prod: `https://adpulse-web-zqlsaqunba-uc.a.run.app`
- Both currently running with `VITE_USE_MOCK_DATA=true` (mock data + mock auth bypass)

### 3. Bug Fixes
- **Login redirect in mock mode** — Login page didn't redirect authenticated users. Fixed by adding `useAuth()` check in `Login.jsx`.
- **Vite env precedence** — `.env.production` overrides `.env`. Had to edit `.env.production` directly.
- **Cloud Build caching** — Stale Docker layers served old bundles. Added Dockerfile comment to bust cache.
- **Null data crash on Channels tab** — `ChannelsTab` received `null` on first render before `useEffect`. Added `|| { channels: [], data: [] }` fallback in `AcquisitionPage.tsx`.

### 4. GitHub Push
- `development` branch pushed to `gurudevkaranth/adpulse-portal` (collaborator access granted for `jk-ootb`)

### 5. Phase 2 Planning
- Created design doc: `docs/plans/2026-03-09-auth-dns-contributing-design.md`
- Created implementation plan: `docs/plans/2026-03-09-auth-dns-contributing-plan.md`
- Created 10 beads tasks for Phase 2
- Created 3 worktrees for Phase 2 agents
- Agent 1 launched briefly but no commits made before session paused

### 6. saas-backend
- `DEV-6636` branch: All 6 tasks complete (health scoring endpoint)
- PRs not yet created (pending session resume)

---

## Current Git State

### adpulse-portal
- **Branch:** `development` at `0dd93fe`
- **Remote:** `https://github.com/gurudevkaranth/adpulse-portal.git` (push access confirmed)
- **Pushed:** Yes, `development` branch is on remote

### Worktrees (7 total — 4 stale from Phase 1, 3 new for Phase 2)

**Phase 1 (stale, can be cleaned up):**
```
.claude/worktrees/api-integration  feat/api-integration  (already merged)
.claude/worktrees/ts-cicd          feat/ts-cicd          (already merged)
.claude/worktrees/ui-polish        feat/ui-polish        (already merged)
```

**Phase 2 (active, no commits yet):**
```
.claude/worktrees/real-auth        feat/real-auth        (Agent 1: auth + env)
.claude/worktrees/cicd-dns         feat/cicd-dns         (Agent 2: CI/CD + DNS)
.claude/worktrees/contributing     feat/contributing     (Agent 3: docs)
```

### saas-backend
- **Branch:** `DEV-6636` (all tasks complete, pushed to origin)
- **PRs:** Not yet created (DEV-6636 → develop, DEV-6636 → main)

---

## Phase 2 Beads Tasks (11 open)

| ID | Task | Agent | Priority |
|---|---|---|---|
| AP-d7k | Remove mock auth bypass from AuthProvider | Agent 1 | P1 |
| AP-6mg | Update env files for real auth | Agent 1 | P1 |
| AP-zra | Clean up Dockerfile | Agent 1 | P2 |
| AP-8pe | Fix CI/CD workflow for build-time env vars | Agent 2 | P1 |
| AP-c0i | Set up Cloud Run domain mappings | Agent 2 | P1 |
| AP-n83 | Set up Cloudflare DNS records | Agent 2 | P1 |
| AP-pej | Register OAuth redirect URIs | Agent 2 | P1 |
| AP-r1w | Create CONTRIBUTING.md | Agent 3 | P2 |
| AP-bdh | Update CLAUDE.md for real auth | Agent 3 | P2 |
| AP-d8n | Redeploy QA and Prod with real auth | Agent 3 | P1 |
| AP-6gg | Write integration tests (from Phase 1) | — | P2 |

All 11 tasks are ready (no blockers).

---

## Phase 2 Design Decisions

| Question | Decision |
|---|---|
| Auth approach | Always real OAuth — no mock bypass |
| QA auth backend | Separate — `qa.app.outoftheblue.ai` |
| DNS provider | Cloudflare CNAME to `ghs.googlehosted.com`, DNS-only |
| Mock data flag | Only controls data source, not auth |
| WIF secrets | Already configured in repo |

---

## Cloud Run Services

| Service | URL | Status |
|---|---|---|
| `adpulse-web-qa` | `https://adpulse-web-qa-zqlsaqunba-uc.a.run.app` | Live (mock data + mock auth) |
| `adpulse-web` | `https://adpulse-web-zqlsaqunba-uc.a.run.app` | Live (mock data + mock auth) |

**Pending DNS mappings:**
- `adpulse.app.outoftheblue.ai` → `adpulse-web`
- `qa.adpulse.app.outoftheblue.ai` → `adpulse-web-qa`

---

## Key Files

| File | Purpose |
|---|---|
| `docs/plans/2026-03-09-auth-dns-contributing-design.md` | Phase 2 design (approved) |
| `docs/plans/2026-03-09-auth-dns-contributing-plan.md` | Phase 2 implementation plan (11 tasks, 3 agents) |
| `docs/plans/2026-03-08-adpulse-core-implementation.md` | Phase 1 plan (complete) |
| `docs/RECOVERY-2026-03-08.md` | Phase 1 recovery file |
| `docs/RUNBOOK-tmux-agents.md` | tmux + agent teams runbook |
| `docs/LEARNINGS.md` | Issues log (8 entries) |
| `CLAUDE.md` | Project conventions |
| `STYLE_GUIDE.md` | Design system reference |

---

## How to Resume

```bash
# 1. Go to adpulse-portal
cd ~/Work/dev/adpulse-portal

# 2. Verify beads state
bd stats && bd ready

# 3. Clean up Phase 1 worktrees (already merged)
git worktree remove .claude/worktrees/api-integration
git worktree remove .claude/worktrees/ts-cicd
git worktree remove .claude/worktrees/ui-polish

# 4. Verify Phase 2 worktrees exist
git worktree list

# 5. Create tmux session with 3 panes
tmux new-session -d -s adpulse-impl -n agents
tmux split-window -h -t adpulse-impl:agents
tmux split-window -v -t adpulse-impl:agents.0

# 6. Launch agents in each pane
# Pane 0 — Agent 1: Auth + Env (feat/real-auth)
# Pane 1 — Agent 2: CI/CD + DNS (feat/cicd-dns)
# Pane 2 — Agent 3: Docs + Contributing (feat/contributing)
# See plan doc for exact claude commands per agent

# 7. Attach to tmux
tmux attach -t adpulse-impl

# 8. After agents complete, merge:
# feat/real-auth → development
# feat/cicd-dns → development
# feat/contributing → development
# Push development → triggers CI/CD → QA auto-deploy

# 9. Create saas-backend PRs (still pending):
cd ~/Work/dev/saas-backend
# gh pr create --base develop --title "DEV-6636: Add 6D ad health scoring endpoint"
# gh pr create --base main --title "DEV-6636: Add 6D ad health scoring endpoint"
```

---

## Manual Steps Required (not automatable by agents)

1. **Cloudflare DNS** — Add CNAME records in Cloudflare dashboard (Task AP-n83)
2. **OAuth redirect URIs** — Register new domains in Google/Microsoft/Okta OAuth consoles and backend config (Task AP-pej)
3. **Repo protection rules** — Enable branch protection on `main` and `development` (for gurudevkaranth)
